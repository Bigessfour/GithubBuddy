/** localStorage keys for paths (fork workspace vs course clone). */
export const STORAGE_WORKSPACE = "platoon-companion-workspace";
export const STORAGE_UPSTREAM = "platoon-companion-upstream-path";
